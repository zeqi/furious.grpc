In the furious.grpc directory.

First start service:

$ cd server/node.mongo/

$ npm install

$ node server.js

Second start client：

$ cd client/node/

$ npm install

$ node bin/www    or   npm start
